<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.totra.spring.ex.mvc.repository.UserRepository">
	<insert parameterType="map" id="insertUser">
		INSERT INTO
			`new_user`
		(
			`name`
			,`yyyymmdd`
			,`email`
			,`introduce`
		)
		VALUE
		(
			#{name}
			,#{birthday}
			,#{email}
			,#{introduce}
		);
	</insert>
	
	<select id="selectLastUser" resultType="com.totra.spring.ex.mvc.domain.User">
		SELECT
			`id`
			,`name`
			,`yyyymmdd`
			,`email`
			,`introduce`
			,`createdAt`
			,`updatedAt`
		FROM `new_user`
		ORDER BY `id` DESC
		LIMIT 1;
	</select>
</mapper>